apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: airelgraph
    component: backend
spec:
  type: ClusterIP
  selector:
    app: airelgraph
    component: backend
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
